class Objects:
    # Placing objects into the labyrith
    # Make an object disapper once collected by MacGyver
    def __init__(self):
        self.object_1 = "nidle"
        self.object_2 = "tube"
        self.object_3 = "ether"
        self.object = "syringe"

    def adding_objects(self):
        #install the counter to know if MG has collected all 3 requred objects
        #in order to create a final object.
        
        #random placement of 3 objects
        #counter:
        #while player == moving:
            # if player_placement == none
                #pass
            # else:
                #object counter =+ 1
        